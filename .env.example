# 复制本文件为 .env 后按需修改；.env 不会进入版本控制

# 接口基础地址（Apifox Mock 服务地址，默认本地占位）
VITE_API_BASE_URL=/api

# 接口请求超时（毫秒），超出按网络失败走 LocalStorage 兜底
VITE_API_TIMEOUT=8000

# Mock 服务端口（Apifox 默认 9000，按实际填写）
VITE_MOCK_PORT=9000
